<!DOCTYPE html>
<html lang="en">
    <head>
        <title>Humancredit - Banner {{ entity.id }}</title>
        <style type="text/css">
            * {
                margin: 0;
                padding: 0;
                border: 0;
                overflow: hidden;
            }

            .hide {
                display: none
            }
        </style>
    </head>

    <body>
        <a target="_top" href="{{ signedLink }}">
            <img src="{{ signedImage }}" />
        </a>
        <a href="{{ signedFeedback }}" class="hide">Feedback</a>
    </body>
</html>