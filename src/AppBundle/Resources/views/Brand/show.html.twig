{% extends 'AppBundle::base.html.twig' %}

{% block body -%}
<h2>Brand</h2>

<table class="record_properties">
    <tbody>
        <tr>
            <th>Id</th>
            <td>{{ entity.id }}</td>
        </tr>
        <tr>
            <th>Name</th>
            <td>{{ entity.name }}</td>
        </tr>
    </tbody>
</table>

<hr />

<h3>Random Banner(id: {{ banner.id }})</h3>

<p>
    <img src="{{ signedUrl }}" />
</p>

<ul class="record_actions">
    <li>
        <a href="{{ path('brand') }}"> Back to the list </a>
    </li>
</ul>
{% endblock %}
